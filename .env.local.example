# Copy this file to .env.local and fill in the real key.
# .env.local is gitignored — it never gets committed. This template does.
#
# Used only by the optional `dispatch` path of harvest/scripts/run_judge.py
# (the API judge run). The offline prepare -> tally flow needs no credentials.

ANTHROPIC_API_KEY=sk-ant-...
